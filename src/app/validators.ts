import {Validators} from "@angular/forms";

export class Validator extends Validators {

  // static minLength(length: number) {
  //   return (control) =>
  //     super.minLength(length)(control) ? {minLength: 'გთხოვთ შეიყვანოთ მინიმუმ ' + length + ' სიმბოლო'}
  //       : undefined;
  // }

}
